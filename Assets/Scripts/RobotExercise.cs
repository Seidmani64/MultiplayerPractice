using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RobotExercise : MonoBehaviour
{
    Robot r;
    // Start is called before the first frame update
    void Start()
    {
       //r->Init();
    }

    // Update is called once per frame
    void Update()
    {
        //r->Draw();
    }
}
